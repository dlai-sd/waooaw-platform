Claim ID:        C-084
Type:            ARCHITECTURAL_CONSTRAINT
Statement:       In any multi-step autonomous workflow, a step MUST NOT begin execution if
                 its declared upstream dependency produced a FAIL, HALT, or ERROR signal in
                 the current workflow instance. Committing, publishing, or transmitting the
                 output of a downstream step when its upstream dependency has failed is a
                 constitutional violation. The downstream step's output is unverifiable
                 because its inputs are invalid. This applies to: the autonomous sprint
                 runner (task N+1 must not execute if scaffold task N fails), customer-facing
                 agents (send_email step must not execute if fetch_patient_list failed), and
                 all Temporal workflows on the WAOOAW platform.

                 Implementation requirement: the runner or orchestrator MUST check the
                 boundary signal (C-083) of step N before initiating step N+1. If the signal
                 shows FAILURE, the downstream step MUST be skipped and a DEPENDENCY_HALT
                 signal emitted explaining which upstream step caused the halt.

Confidence:      97%
Source:          C-083 (Emit-Transport-Listen — boundary signals enable dependency checking);
                 C-023 (Evidence First — committed output of a failed workflow has no valid
                 evidence chain); C-059 (Traceability — code cannot trace to a valid spec
                 if the compilation foundation failed); IB-020 pipeline evidence: WC012-02
                 and WC012-04 were committed to the sprint branch when WC012-01 (scaffold)
                 had failed 3 times. The resulting build errors required 3 hours of human
                 intervention to diagnose and clean up.

Depends On:      C-083 (Emit-Transport-Listen — signals enable dependency checking),
                 C-023 (Evidence First), C-070 (Constitutional DNA)

Produces:        CCT-DEP-01 (verifies that when scaffold task fails, downstream tasks are
                 skipped and DEPENDENCY_HALT signal is emitted); runner SCAFFOLD_TASKS
                 frozenset (explicit declaration of scaffold tasks per sprint);
                 Temporal workflow dependency chain specification

Contradicted By: NONE
Status:          RATIFIED — 2026-07-24 (Founder authorization — Yogesh Khandge)
