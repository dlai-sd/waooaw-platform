Claim ID:        C-085
Type:            ARCHITECTURAL_CONSTRAINT
Statement:       Before executing any workflow step that produces external side effects
                 (email sent, post published, trade placed, code committed, evidence
                 written), the agent MUST verify that a SUCCESS signal for this exact step
                 does not already exist in the current workflow instance. If a SUCCESS
                 signal exists, the step MUST be skipped and the existing signal ID
                 returned as the result. Temporal retries, cron re-runs, and session
                 resumptions are all subject to this obligation. Re-executing a completed
                 step is a constitutional violation that produces: duplicate external
                 effects (two emails to Dr. Mehta's patients, two trade orders for the
                 same signal), duplicate Evidence Ledger records that pollute the audit
                 trail (C-027), and loss of the customer's trust (C-002).

                 Implementation: before any tool call, the agent MUST query its session's
                 Evidence Ledger (or boundary signal stream per C-083) with the idempotency
                 key for this step. The idempotency key is: session_id + step_name +
                 input_hash. If a SUCCESS record with this key exists, skip.

Confidence:      97%
Source:          C-027 (append-only ledger — duplicate records violate audit integrity);
                 C-083 (Emit-Transport-Listen — signals are the source for idempotency
                 checking); C-023 (Evidence First — completed steps have evidence; that
                 evidence must be checked before re-execution); IB-020 pipeline evidence:
                 WC012-01 was re-executed 5 times across 3 cron runs because tasks_done
                 was never written to main branch. Each re-execution produced duplicate
                 git commits and new false spec-gap issues.

Depends On:      C-083 (signals provide the idempotency record),
                 C-027 (append-only ledger — idempotency records are never deleted),
                 C-023 (Evidence First), C-070 (Constitutional DNA)

Produces:        CCT-IDP-01 (verifies that a step with an existing SUCCESS record is
                 skipped on retry, not re-executed); idempotency key specification in
                 architecture/reference/agents/IDEMPOTENCY-KEYS.md; Temporal workflow
                 idempotency activity wrapper

Contradicted By: NONE
Status:          RATIFIED — 2026-07-24 (Founder authorization — Yogesh Khandge)
