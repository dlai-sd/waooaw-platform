Claim ID:        C-072
Type:            OBLIGATION + PLATFORM_INTEGRITY
Statement:       Every line of code written for the WAOOAW platform must conform to the WAOOAW Coding Standards (standards/CODING-STANDARDS.md). Standards cover five dimensions: cosmetic (formatting, naming, style), performance (no blocking hot paths, no N+1 queries, bundle budgets), security (OWASP Top 10 compliance, no PII in logs, parameterized queries only), observability (structured logging with constitutional context, OTel spans on every operation), and unit test quality (AAA pattern, no flaky tests, constitutional basis declared). Standards are enforced by automated linters and formatters in CI — no human code reviewer may approve a PR that fails automated style gates. An agent that writes code not conforming to these standards is producing an artifact that does not represent the WAOOAW institution's professional quality. Cosmetic inconsistency is not a minor issue — it degrades readability for every future agent that reads the code, compounding across the platform's lifetime.
Confidence:      100%
Source:          C-071 (Quality Obligation — coding standards are the first quality layer); C-059 (Implementation Traceability — conformant code is traceable code); C-069 (Self-Improvement — code quality metrics feed the improvement loop); GENESIS Engineering Quality Mandate; standards/CODING-STANDARDS.md.
Depends On:      C-071 (Quality Obligation), C-059 (Traceability), C-065 (SDLC Separation — reviewer validates standards compliance independently)
Produces:        standards/CODING-STANDARDS.md, .editorconfig, pyproject.toml (root), .github/workflows/code-quality.yaml
Contradicted By: NONE
Status:          RATIFIED — 2026-07-19 (Founder authorization)
Reviewer Notes:  WHY COSMETIC STANDARDS ARE CONSTITUTIONAL:

                 A function named `processStuff()` that handles Evidence First enforcement
                 is a constitutional artifact named badly. It will confuse every future agent
                 that reads it. The cognitive overhead of decoding poorly named, inconsistently
                 formatted code is a real cost — measured in tokens spent by LLM agents
                 re-parsing context, in mistakes made by junior agents who misread intent,
                 in time spent by Sujay trying to understand a PR.

                 WAOOAW AI Agents write code for other WAOOAW AI Agents to read.
                 The audience is not a human who can "just figure it out."
                 The audience is an LLM reading a 200-line context window.
                 Clarity, consistency, and constitutional naming ARE performance optimizations.

                 THE FIVE DIMENSIONS:
                 1. COSMETIC: Code reads like it was written by one disciplined professional.
                    No debates about formatting — tools decide, not agents.
                 2. PERFORMANCE: The platform has constitutional latency floors (C-001: 250ms,
                    CE: 40ms). Performance anti-patterns are constitutional violations.
                 3. SECURITY: Every OWASP Top 10 category has a code-level countermeasure.
                    Security is built in, not bolted on.
                 4. OBSERVABILITY: Every operation has a structured log entry and OTel span.
                    Without this, constitutional audit (C-023) is unreliable.
                 5. UNIT TEST QUALITY: Tests are first-class code. Same standards apply.
                    A poorly written test is worse than no test — it creates false confidence.
