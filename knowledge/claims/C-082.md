Claim ID:        C-082
Type:            OBLIGATION + QUALITY_GATE
Statement:       The autonomous sprint runner MUST implement compile or build validation for every language and framework used by any service in the WAOOAW platform. A technology stack added to the platform without a corresponding validation path in validate_written_files() is a constitutional violation of this claim and C-076 (Coverage Obligation). Generated code that cannot be independently verified by the runner MUST NOT be committed. Validation must cover: Python (py_compile + import check), .NET/C# (dotnet build), TypeScript/JavaScript (tsc --noEmit), and any future stack added with a Work Contract sprint.
Confidence:      100%
Source:          C-076 (Coverage Obligation — 90% test coverage is impossible if the code does not compile; a compilation gate is the floor of all coverage obligations); C-059 (Traceability — committed code must be traceable to a verified state; unverified code has no valid trace); C-065 (SDLC Separation — the reviewer cannot approve what the runner has not validated); IB-020 evidence: WC-012 runs — Python code correctly validated via py_compile; TypeScript (WC-016, future) has ZERO validation path in current runner — commits could silently contain non-compiling code.
Depends On:      C-076 (Coverage), C-059 (Traceability), C-065 (SDLC Separation)
Produces:        validate_written_files() extension plan: Python (done), .NET (done), TypeScript (required before WC-016), Flutter/Dart (required before mobile sprint); architecture/reference/dotfiles/ reference configs per stack; runner pre-sprint check: "does a validation path exist for this sprint's tech stack?"; CCT-VAL-01 (meta-test: verify validate_written_files covers all stacks in ARCHITECTURE.md)
Contradicted By: NONE
Status:          RATIFIED — 2026-07-23 (Evidence-based: WC-012 TypeScript gap identified; Founder authorization: "yes please" to proactive fixes session)
Reviewer Notes:  VALIDATION COVERAGE MATRIX (as of 2026-07-23):

                 | Stack           | Validation     | Status       | Sprint needed  |
                 |-----------------|----------------|--------------|----------------|
                 | Python .py      | py_compile     | ✅ DONE      | WC-011+        |
                 | .NET C#         | dotnet build   | ✅ DONE      | WC-012+        |
                 | TypeScript .ts  | tsc --noEmit   | ❌ MISSING   | Required WC-016|
                 | Next.js .tsx    | tsc --noEmit   | ❌ MISSING   | Required WC-016|
                 | Dart/Flutter    | flutter build  | ❌ MISSING   | Required mobile|
                 | SQL migrations  | psql --dry-run | ❌ MISSING   | Required WC-013|

                 IMPLEMENTATION PRIORITY:
                 TypeScript validation must be added to validate_written_files() before WC-016
                 begins. This is a prerequisite, not an optional improvement. Adding it after
                 the sprint begins would mean the first scaffold task runs without validation,
                 repeating the WC-012 pattern.

                 SQL MIGRATION VALIDATION:
                 PostgreSQL migrations (.sql files) cannot be validated without a running
                 database, but structural checks are possible:
                 - Verify file starts with valid DDL (CREATE TABLE, ALTER TABLE, etc.)
                 - Verify no DROP DATABASE or TRUNCATE in migration files
                 - Verify Flyway naming convention (V001__, V002__, etc.)

                 MOBILE EXTENSION:
                 When a mobile (Flutter) sprint is authorized:
                 1. `flutter analyze` must be added to validate_written_files()
                 2. A reference pubspec.yaml must exist in architecture/reference/dotfiles/
                 3. Both must be verified passing in SIM-PL-001 before the sprint begins
                 4. The Android/iOS build validation is out-of-scope for CI (too slow) but
                    a `flutter build apk --debug` dry-run is acceptable for structural check.
