# Dockerfile.test-runner
# constitutional_basis: C-080 (Docker Test Isolation — RATIFIED 2026-07-23)
# ib_item: IB-009
# Implements: tests/QA-STRATEGY.md §Docker Execution Mandate
#
# This is the SINGLE AUTHORITATIVE test execution environment for WAOOAW.
# All tests — unit, integration, CCT, performance, security, pipeline — run here.
# Virtual environments are PROHIBITED (C-080). This image IS the environment.
#
# Usage:
#   docker compose run --rm test-runner pytest tests/
#   docker compose run --rm test-runner pytest tests/constitutional/pipeline/ -v
#   docker compose run --rm test-runner python3 -m py_compile scripts/runner.py
#   docker compose run --rm test-runner ruff check .
#   docker compose run --rm test-runner mypy src/

FROM mcr.microsoft.com/playwright:v1.62.1-noble@sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e

# Security: run as non-root with writable browser and font caches.
RUN (getent group 1000 >/dev/null || groupadd --gid 1000 waooaw) \
    && (getent passwd 1000 >/dev/null || useradd --uid 1000 --gid 1000 --create-home waooaw)

WORKDIR /workspace

# System dependencies
RUN --mount=type=cache,id=wc104-full-apt-lists,target=/var/lib/apt/lists,sharing=locked \
    --mount=type=cache,id=wc104-full-apt-packages,target=/var/cache/apt,sharing=locked \
    apt-get -o Acquire::Retries=5 -o Acquire::http::Timeout=30 -o Acquire::https::Timeout=30 update \
    && apt-get -o Acquire::Retries=5 -o Acquire::http::Timeout=30 -o Acquire::https::Timeout=30 install -y --no-install-recommends \
    git \
    gh \
    curl \
    docker.io \
    docker-buildx \
    docker-compose-v2 \
    jq \
    python3 \
    python3-pip \
    python-is-python3

# .NET SDK for CE + BP .NET tests
RUN curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- --channel 9.0 --install-dir /usr/local/dotnet
ENV DOTNET_ROOT=/usr/local/dotnet
ENV PATH=$PATH:/usr/local/dotnet

# Web acceptance toolchain — exact versions and browser binaries are part of the
# authoritative environment, not a per-session setup step.
ENV PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
RUN npm install --global pnpm@9.15.9
COPY --chown=waooaw:waooaw web/package.json web/pnpm-lock.yaml /opt/waooaw-web/
RUN --mount=type=cache,id=wc104-full-pnpm,target=/root/.local/share/pnpm/store,sharing=locked \
    cd /opt/waooaw-web && pnpm install --frozen-lockfile
ENV NODE_PATH=/opt/waooaw-web/node_modules \
    PATH=/opt/waooaw-web/node_modules/.bin:$PATH

# Python test dependencies — installed to system Python (no venv per C-080)
# This file is the venv replacement — the image IS the isolated environment.
COPY requirements-test.txt /workspace/requirements-test.txt
RUN --mount=type=cache,id=wc104-full-pip,target=/root/.cache/pip,sharing=locked \
    pip install --break-system-packages -r requirements-test.txt

USER 1000:1000

# Default: run all constitutional CCTs
CMD ["python3", "-m", "pytest", "tests/constitutional/", "-v", "--tb=short"]
