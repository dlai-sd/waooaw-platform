name: GOAL-006 Deploy Environment

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      release_run_id:
        required: true
        type: number
      release_sha:
        required: true
        type: string
      apply:
        required: true
        type: boolean

permissions:
  contents: read
  actions: read
  id-token: write

jobs:
  terraform:
    name: ${{ inputs.apply && 'Apply' || 'Plan' }} ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    env:
      ARM_CLIENT_ID: ${{ vars.GOAL006_BOOTSTRAP_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.GOAL006_AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.GOAL006_AZURE_SUBSCRIPTION_ID }}
      TFSTATE_RESOURCE_GROUP: ${{ vars.GOAL006_TFSTATE_RESOURCE_GROUP }}
      TFSTATE_STORAGE_ACCOUNT: ${{ vars.GOAL006_TFSTATE_STORAGE_ACCOUNT }}
      TFSTATE_CONTAINER: ${{ vars.GOAL006_TFSTATE_CONTAINER }}
      TF_VAR_tfstate_resource_group: ${{ vars.GOAL006_TFSTATE_RESOURCE_GROUP }}
      TF_VAR_tfstate_storage_account: ${{ vars.GOAL006_TFSTATE_STORAGE_ACCOUNT }}
      TF_VAR_tfstate_container: ${{ vars.GOAL006_TFSTATE_CONTAINER }}
      TF_VAR_tfstate_storage_account_id: ${{ vars.GOAL006_TFSTATE_STORAGE_ACCOUNT_ID }}
      WORKLOAD_CONFIGURATION: ${{ vars.GOAL006_WORKLOAD_CONFIGURATION }}
    steps:
      - name: Enforce caller and Production plan-only boundary
        env:
          CALLER_WORKFLOW_REF: ${{ github.workflow_ref }}
          EXPECTED_CALLER_WORKFLOW_REF: ${{ github.repository }}/.github/workflows/promote.yaml@refs/heads/main
          TARGET_ENVIRONMENT: ${{ inputs.environment }}
          APPLY_REQUESTED: ${{ inputs.apply }}
        run: |
          test "$CALLER_WORKFLOW_REF" = "$EXPECTED_CALLER_WORKFLOW_REF"
          case "$TARGET_ENVIRONMENT" in
            demo|uat|prod) ;;
            *) echo "Unsupported environment: $TARGET_ENVIRONMENT" >&2; exit 1 ;;
          esac
          if [[ "$TARGET_ENVIRONMENT" == "prod" && "$APPLY_REQUESTED" == "true" ]]; then
            echo "Production is plan-only under FA-052" >&2
            exit 1
          fi

      - name: Reject stale release before cloud access
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_SHA: ${{ inputs.release_sha }}
        run: |
          latest_main_sha=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')
          test "$RELEASE_SHA" = "$latest_main_sha"

      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_sha }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8

      - uses: docker/setup-buildx-action@v3

      - name: Download signed exact-six tuple
        uses: actions/download-artifact@v4
        with:
          pattern: goal006-exact-six-release-*
          path: release-input
          repository: ${{ github.repository }}
          run-id: ${{ inputs.release_run_id }}
          github-token: ${{ github.token }}

      - name: Verify tuple and GitHub attestation
        run: |
          manifest=$(find release-input -name registry-release-manifest.json -type f -print -quit)
          evidence=$(find release-input -type d -name evidence -print -quit)
          test -n "$manifest"
          test -n "$evidence"
          python scripts/goal006_registry_manifest.py --verify-manifest "$manifest" \
            --expected-source-commit '${{ inputs.release_sha }}' \
            --expected-run-id '${{ inputs.release_run_id }}' \
            --evidence-directory "$evidence"
          gh attestation verify "$manifest" --repo "$GITHUB_REPOSITORY"
          while IFS= read -r image; do
            gh attestation verify "oci://$image" --repo "$GITHUB_REPOSITORY"
          done < <(jq -r '.images | to_entries[] | .value' "$manifest")
          cp "$manifest" registry-release-manifest.json
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Verify anonymous exact-six GHCR pulls and validate environment inputs
        run: |
          while IFS= read -r image; do
            docker buildx imagetools inspect "$image" >/dev/null
          done < <(jq -r '.images | to_entries[] | .value' registry-release-manifest.json)
          test -n "$WORKLOAD_CONFIGURATION"
          printf '%s' "$WORKLOAD_CONFIGURATION" > workload-configuration.json
          python scripts/goal006_deployment_inputs.py \
            --environment '${{ inputs.environment }}' \
            --manifest registry-release-manifest.json \
            --configuration workload-configuration.json \
            --output workload.auto.tfvars.json \
            --ghcr-packages-public-verified

      - name: Azure login with constrained bootstrap OIDC identity
        uses: azure/login@v2
        with:
          client-id: ${{ env.ARM_CLIENT_ID }}
          tenant-id: ${{ env.ARM_TENANT_ID }}
          subscription-id: ${{ env.ARM_SUBSCRIPTION_ID }}

      - name: Enforce subscription and budget boundary
        run: |
          test "$ARM_SUBSCRIPTION_ID" = "2ed11839-6a0f-4eaa-bd94-44ca96ff5d84"
          test "$TFSTATE_RESOURCE_GROUP" = "waooaw-platform-rg"
          test "$TFSTATE_STORAGE_ACCOUNT" = "waooawp3tfstate2ed118"
          test "$TFSTATE_CONTAINER" = "tfstate"
          test "$TF_VAR_tfstate_storage_account_id" = "/subscriptions/2ed11839-6a0f-4eaa-bd94-44ca96ff5d84/resourceGroups/waooaw-platform-rg/providers/Microsoft.Storage/storageAccounts/waooawp3tfstate2ed118"
          amount=$(az rest --method get \
            --url "https://management.azure.com/subscriptions/$ARM_SUBSCRIPTION_ID/providers/Microsoft.Consumption/budgets/waooaw-phase3-monthly?api-version=2023-11-01" \
            --query properties.amount -o tsv)
          test "$amount" = "10000.0" || test "$amount" = "10000"
          scope="/subscriptions/$ARM_SUBSCRIPTION_ID"
          query='{"type":"ActualCost","timeframe":"MonthToDate","dataset":{"granularity":"None","aggregation":{"totalCost":{"name":"PreTaxCost","function":"Sum"}}}}'
          az rest --method post --url "https://management.azure.com${scope}/providers/Microsoft.CostManagement/query?api-version=2023-11-01" --body "$query" > actual-cost.json
          forecast='{"type":"Usage","timeframe":"MonthToDate","includeActualCost":true,"includeFreshPartialCost":true,"dataset":{"granularity":"None","aggregation":{"totalCost":{"name":"PreTaxCost","function":"Sum"}}}}'
          az rest --method post --url "https://management.azure.com${scope}/providers/Microsoft.CostManagement/forecast?api-version=2023-11-01" --body "$forecast" > forecast-cost.json
          python scripts/goal006_cost_gate.py \
            --actual actual-cost.json \
            --forecast forecast-cost.json \
            --configuration workload-configuration.json

      - name: Open temporary state firewall rule
        run: |
          runner_ip=$(curl --fail --silent --show-error https://api.ipify.org)
          echo "RUNNER_IP=$runner_ip" >> "$GITHUB_ENV"
          az storage account network-rule add \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --ip-address "$runner_ip" --output none

      - name: Terraform foundation plan
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/foundation
        run: |
          terraform init -input=false \
            -backend-config="resource_group_name=$TFSTATE_RESOURCE_GROUP" \
            -backend-config="storage_account_name=$TFSTATE_STORAGE_ACCOUNT" \
            -backend-config="container_name=$TFSTATE_CONTAINER" \
            -backend-config="use_oidc=true" \
            -backend-config="use_azuread_auth=true"
          terraform validate
          terraform plan -input=false -lock-timeout=5m -out=foundation.tfplan

      - name: Terraform foundation apply
        if: inputs.apply
        id: foundation
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/foundation
        run: |
          latest_main_sha=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')
          test '${{ inputs.release_sha }}' = "$latest_main_sha"
          terraform apply -input=false -auto-approve foundation.tfplan
          echo "deployment_client_id=$(terraform output -raw deployment_client_id)" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Azure login with environment deployment OIDC identity
        if: inputs.apply
        uses: azure/login@v2
        with:
          client-id: ${{ steps.foundation.outputs.deployment_client_id }}
          tenant-id: ${{ env.ARM_TENANT_ID }}
          subscription-id: ${{ env.ARM_SUBSCRIPTION_ID }}

      - name: Terraform workload plan
        if: inputs.apply
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/workload
        run: |
          cp "$GITHUB_WORKSPACE/workload.auto.tfvars.json" .
          terraform init -input=false \
            -backend-config="resource_group_name=$TFSTATE_RESOURCE_GROUP" \
            -backend-config="storage_account_name=$TFSTATE_STORAGE_ACCOUNT" \
            -backend-config="container_name=$TFSTATE_CONTAINER" \
            -backend-config="use_oidc=true" \
            -backend-config="use_azuread_auth=true"
          terraform validate
          terraform plan -input=false -lock-timeout=5m -out=workload.tfplan

      - name: Terraform workload apply
        if: inputs.apply
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/workload
        run: |
          latest_main_sha=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')
          test '${{ inputs.release_sha }}' = "$latest_main_sha"
          terraform apply -input=false -auto-approve workload.tfplan
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Upload plans and evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: goal006-${{ inputs.environment }}-${{ inputs.apply && 'apply' || 'plan' }}-${{ inputs.release_run_id }}
          path: |
            registry-release-manifest.json
            infrastructure/terraform/phase2/environments/${{ inputs.environment }}/foundation/foundation.tfplan
            infrastructure/terraform/phase2/environments/${{ inputs.environment }}/workload/workload.tfplan
          if-no-files-found: warn
          retention-days: 90

      - name: Reauthenticate bootstrap identity for cleanup
        if: always() && env.RUNNER_IP != ''
        uses: azure/login@v2
        with:
          client-id: ${{ env.ARM_CLIENT_ID }}
          tenant-id: ${{ env.ARM_TENANT_ID }}
          subscription-id: ${{ env.ARM_SUBSCRIPTION_ID }}

      - name: Close state firewall rule
        if: always() && env.RUNNER_IP != ''
        run: |
          az storage account network-rule remove \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --ip-address "$RUNNER_IP" --output none