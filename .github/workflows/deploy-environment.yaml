name: GOAL-006 Deploy Environment

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      release_run_id:
        required: true
        type: number
      release_sha:
        required: true
        type: string
      apply:
        required: true
        type: boolean
    outputs:
      verification_client_id:
        value: ${{ jobs.terraform.outputs.verification_client_id }}
      web_url:
        value: ${{ jobs.terraform.outputs.web_url }}

permissions:
  contents: read
  actions: read
  id-token: write

jobs:
  terraform:
    name: ${{ inputs.apply && 'Apply' || 'Plan' }} ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    outputs:
      verification_client_id: ${{ steps.foundation.outputs.verification_client_id }}
      web_url: ${{ steps.workload.outputs.web_url }}
    env:
      ARM_CLIENT_ID: ${{ vars.WAOOAW_PLATFORM_BOOTSTRAP_CLIENT_ID }}
      ARM_TENANT_ID: ${{ vars.WAOOAW_PLATFORM_AZURE_TENANT_ID }}
      ARM_SUBSCRIPTION_ID: ${{ vars.WAOOAW_PLATFORM_AZURE_SUBSCRIPTION_ID }}
      TFSTATE_RESOURCE_GROUP: ${{ vars.WAOOAW_PLATFORM_TFSTATE_RESOURCE_GROUP }}
      TFSTATE_STORAGE_ACCOUNT: ${{ vars.WAOOAW_PLATFORM_TFSTATE_STORAGE_ACCOUNT }}
      TFSTATE_CONTAINER: ${{ vars.WAOOAW_PLATFORM_TFSTATE_CONTAINER }}
      TF_VAR_tfstate_resource_group: ${{ vars.WAOOAW_PLATFORM_TFSTATE_RESOURCE_GROUP }}
      TF_VAR_tfstate_storage_account: ${{ vars.WAOOAW_PLATFORM_TFSTATE_STORAGE_ACCOUNT }}
      TF_VAR_tfstate_container: ${{ vars.WAOOAW_PLATFORM_TFSTATE_CONTAINER }}
      TF_VAR_tfstate_storage_account_id: ${{ vars.WAOOAW_PLATFORM_TFSTATE_STORAGE_ACCOUNT_ID }}
      WORKLOAD_CONFIGURATION: ${{ vars.WAOOAW_PLATFORM_WORKLOAD_CONFIGURATION }}
      SEEDER_IMAGE: mcr.microsoft.com/azure-cli@sha256:4faeb3c955086c3842d4f8cf0ff1d900ce3a1c68c6e6c6430c5e8a3cb882c5aa
      SEEDER_JOB: goal006-secret-seeder
    steps:
      - name: Enforce current Demo-only authorization
        env:
          CALLER_WORKFLOW_REF: ${{ github.workflow_ref }}
          EXPECTED_CALLER_WORKFLOW_REF: ${{ github.repository }}/.github/workflows/deploy-demo.yaml@refs/heads/main
          TARGET_ENVIRONMENT: ${{ inputs.environment }}
          APPLY_REQUESTED: ${{ inputs.apply }}
        run: |
          test "$CALLER_WORKFLOW_REF" = "$EXPECTED_CALLER_WORKFLOW_REF"
          test "$TARGET_ENVIRONMENT" = "demo"
          test "$APPLY_REQUESTED" = "true"

      - name: Reject stale release before cloud access
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_SHA: ${{ inputs.release_sha }}
        run: |
          latest_main_sha=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')
          test "$RELEASE_SHA" = "$latest_main_sha"

      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.release_sha }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8

      - uses: docker/setup-buildx-action@v3

      - name: Download signed exact-six tuple
        uses: actions/download-artifact@v4
        with:
          pattern: goal006-exact-six-release-*
          path: release-input
          repository: ${{ github.repository }}
          run-id: ${{ inputs.release_run_id }}
          github-token: ${{ github.token }}

      - name: Verify tuple and GitHub attestation
        run: |
          manifest=$(find release-input -name registry-release-manifest.json -type f -print -quit)
          evidence=$(find release-input -type d -name evidence -print -quit)
          test -n "$manifest"
          test -n "$evidence"
          python scripts/goal006_registry_manifest.py --verify-manifest "$manifest" \
            --expected-source-commit '${{ inputs.release_sha }}' \
            --expected-run-id '${{ inputs.release_run_id }}' \
            --evidence-directory "$evidence"
          gh attestation verify "$manifest" --repo "$GITHUB_REPOSITORY"
          while IFS= read -r image; do
            gh attestation verify "oci://$image" --repo "$GITHUB_REPOSITORY"
          done < <(jq -r '.images | to_entries[] | .value' "$manifest")
          cp "$manifest" registry-release-manifest.json
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Verify anonymous exact-six GHCR pulls and configuration presence
        run: |
          while IFS= read -r image; do
            docker buildx imagetools inspect "$image" >/dev/null
          done < <(jq -r '.images | to_entries[] | .value' registry-release-manifest.json)
          test -n "$WORKLOAD_CONFIGURATION"
          printf '%s' "$WORKLOAD_CONFIGURATION" > workload-configuration.json
          jq -e '.founder_ipv4_cidr and .planned_incremental_monthly_cost_inr != null and .cumulative_one_time_cost_inr != null' \
            workload-configuration.json >/dev/null

      - name: Azure login with constrained bootstrap OIDC identity
        uses: azure/login@v2
        with:
          client-id: ${{ env.ARM_CLIENT_ID }}
          tenant-id: ${{ env.ARM_TENANT_ID }}
          subscription-id: ${{ env.ARM_SUBSCRIPTION_ID }}

      - name: Enforce subscription and budget boundary
        run: |
          test "$ARM_SUBSCRIPTION_ID" = "2ed11839-6a0f-4eaa-bd94-44ca96ff5d84"
          test "$TFSTATE_RESOURCE_GROUP" = "waooaw-platform-rg"
          test "$TFSTATE_STORAGE_ACCOUNT" = "waooawp3tfstate2ed118"
          test "$TFSTATE_CONTAINER" = "tfstate"
          test "$TF_VAR_tfstate_storage_account_id" = "/subscriptions/2ed11839-6a0f-4eaa-bd94-44ca96ff5d84/resourceGroups/waooaw-platform-rg/providers/Microsoft.Storage/storageAccounts/waooawp3tfstate2ed118"
          amount=$(az rest --method get \
            --url "https://management.azure.com/subscriptions/$ARM_SUBSCRIPTION_ID/providers/Microsoft.Consumption/budgets/waooaw-phase3-monthly?api-version=2023-11-01" \
            --query properties.amount -o tsv)
          test "$amount" = "10000.0" || test "$amount" = "10000"
          scope="/subscriptions/$ARM_SUBSCRIPTION_ID"
          query='{"type":"ActualCost","timeframe":"MonthToDate","dataset":{"granularity":"None","aggregation":{"totalCost":{"name":"PreTaxCost","function":"Sum"}}}}'
          az rest --method post --url "https://management.azure.com${scope}/providers/Microsoft.CostManagement/query?api-version=2023-11-01" --body "$query" > actual-cost.json
          forecast='{"type":"Usage","timeframe":"MonthToDate","includeActualCost":true,"includeFreshPartialCost":true,"dataset":{"granularity":"None","aggregation":{"totalCost":{"name":"PreTaxCost","function":"Sum"}}}}'
          az rest --method post --url "https://management.azure.com${scope}/providers/Microsoft.CostManagement/forecast?api-version=2023-11-01" --body "$forecast" > forecast-cost.json
          python scripts/goal006_cost_gate.py \
            --actual actual-cost.json \
            --forecast forecast-cost.json \
            --configuration workload-configuration.json

      - name: Open temporary state firewall rule
        run: |
          runner_ip=$(curl --fail --silent --show-error https://api.ipify.org)
          echo "RUNNER_IP=$runner_ip" >> "$GITHUB_ENV"
          az storage account network-rule add \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --ip-address "$runner_ip" --output none

      - name: Terraform foundation plan
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/foundation
        run: |
          terraform init -input=false \
            -backend-config="resource_group_name=$TFSTATE_RESOURCE_GROUP" \
            -backend-config="storage_account_name=$TFSTATE_STORAGE_ACCOUNT" \
            -backend-config="container_name=$TFSTATE_CONTAINER" \
            -backend-config="use_oidc=true" \
            -backend-config="use_azuread_auth=true"
          terraform validate
          terraform plan -input=false -lock-timeout=5m -out=foundation.tfplan

      - name: Terraform foundation apply
        if: inputs.apply
        id: foundation
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/foundation
        run: |
          latest_main_sha=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')
          test '${{ inputs.release_sha }}' = "$latest_main_sha"
          terraform apply -input=false -auto-approve foundation.tfplan
          echo "deployment_client_id=$(terraform output -raw deployment_client_id)" >> "$GITHUB_OUTPUT"
          echo "deployment_identity_id=$(terraform output -raw deployment_identity_id)" >> "$GITHUB_OUTPUT"
          echo "verification_client_id=$(terraform output -raw verification_client_id)" >> "$GITHUB_OUTPUT"
          echo "resource_group_name=$(terraform output -raw resource_group_name)" >> "$GITHUB_OUTPUT"
          echo "container_app_environment_name=$(terraform output -raw container_app_environment_name)" >> "$GITHUB_OUTPUT"
          echo "key_vault_id=$(terraform output -raw key_vault_id)" >> "$GITHUB_OUTPUT"
          echo "key_vault_name=$(terraform output -raw key_vault_name)" >> "$GITHUB_OUTPUT"
          echo "key_vault_uri=$(terraform output -raw key_vault_uri)" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Azure login with environment deployment OIDC identity
        if: inputs.apply
        uses: azure/login@v2
        with:
          client-id: ${{ steps.foundation.outputs.deployment_client_id }}
          tenant-id: ${{ env.ARM_TENANT_ID }}
          subscription-id: ${{ env.ARM_SUBSCRIPTION_ID }}

      - name: Create private digest-pinned runtime secret seeder
        env:
          CONTAINER_APP_ENVIRONMENT: ${{ steps.foundation.outputs.container_app_environment_name }}
          DEPLOYMENT_CLIENT_ID: ${{ steps.foundation.outputs.deployment_client_id }}
          DEPLOYMENT_IDENTITY_ID: ${{ steps.foundation.outputs.deployment_identity_id }}
          KEY_VAULT_NAME: ${{ steps.foundation.outputs.key_vault_name }}
          RESOURCE_GROUP: ${{ steps.foundation.outputs.resource_group_name }}
        run: |
          az containerapp job delete --name "$SEEDER_JOB" --resource-group "$RESOURCE_GROUP" --yes --output none 2>/dev/null || true
          az containerapp job create \
            --name "$SEEDER_JOB" \
            --resource-group "$RESOURCE_GROUP" \
            --environment "$CONTAINER_APP_ENVIRONMENT" \
            --trigger-type Manual \
            --replica-timeout 600 \
            --replica-retry-limit 1 \
            --replica-completion-count 1 \
            --parallelism 1 \
            --image "$SEEDER_IMAGE" \
            --mi-user-assigned "$DEPLOYMENT_IDENTITY_ID" \
            --cpu 0.25 \
            --memory 0.5Gi \
            --env-vars "AZURE_CLIENT_ID=$DEPLOYMENT_CLIENT_ID" "KEY_VAULT_NAME=$KEY_VAULT_NAME" \
            --command /bin/sh \
            --args -c 'set -eu; az login --identity --client-id "$AZURE_CLIENT_ID" --allow-no-subscriptions --output none; for name in constitutional-engine business-platform professional-runtime ai-runtime web billing-engine; do value=$(head -c 48 /dev/urandom | base64 | tr -d "\n"); for attempt in 1 2 3 4 5 6; do if az keyvault secret set --vault-name "$KEY_VAULT_NAME" --name "$name" --value "$value" --output none; then break; fi; test "$attempt" != 6; sleep 10; done; unset value; done' \
            --output none

      - name: Run private runtime secret seeder
        id: seeder
        env:
          RESOURCE_GROUP: ${{ steps.foundation.outputs.resource_group_name }}
        run: |
          execution_name=$(az containerapp job start --name "$SEEDER_JOB" --resource-group "$RESOURCE_GROUP" --query name -o tsv)
          test -n "$execution_name"
          for attempt in $(seq 1 60); do
            status=$(az containerapp job execution show \
              --name "$SEEDER_JOB" \
              --resource-group "$RESOURCE_GROUP" \
              --job-execution-name "$execution_name" \
              --query properties.status -o tsv)
            case "$status" in
              Succeeded)
                az containerapp job execution show --name "$SEEDER_JOB" --resource-group "$RESOURCE_GROUP" \
                  --job-execution-name "$execution_name" > secret-seeder-execution.json
                exit 0
                ;;
              Failed) echo "Private runtime secret seeder failed" >&2; exit 1 ;;
            esac
            test "$attempt" != 60
            sleep 10
          done

      - name: Build workload inputs from trusted foundation outputs
        run: |
          python scripts/goal006_deployment_inputs.py \
            --environment '${{ inputs.environment }}' \
            --manifest registry-release-manifest.json \
            --configuration workload-configuration.json \
            --key-vault-id '${{ steps.foundation.outputs.key_vault_id }}' \
            --key-vault-uri '${{ steps.foundation.outputs.key_vault_uri }}' \
            --output workload.auto.tfvars.json \
            --ghcr-packages-public-verified

      - name: Terraform workload plan
        if: inputs.apply
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/workload
        run: |
          cp "$GITHUB_WORKSPACE/workload.auto.tfvars.json" .
          terraform init -input=false \
            -backend-config="resource_group_name=$TFSTATE_RESOURCE_GROUP" \
            -backend-config="storage_account_name=$TFSTATE_STORAGE_ACCOUNT" \
            -backend-config="container_name=$TFSTATE_CONTAINER" \
            -backend-config="use_oidc=true" \
            -backend-config="use_azuread_auth=true"
          terraform validate
          terraform plan -input=false -lock-timeout=5m -out=workload.tfplan

      - name: Terraform workload apply
        if: inputs.apply
        id: workload
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/workload
        run: |
          latest_main_sha=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')
          test '${{ inputs.release_sha }}' = "$latest_main_sha"
          terraform apply -input=false -auto-approve workload.tfplan
          echo "web_url=$(terraform output -raw web_url)" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Roll back disposable Demo workload after apply failure
        if: failure() && steps.workload.outcome == 'failure'
        working-directory: infrastructure/terraform/phase2/environments/${{ inputs.environment }}/workload
        run: |
          terraform destroy -input=false -auto-approve -lock-timeout=5m

      - name: Delete private runtime secret seeder
        if: always() && steps.foundation.outcome == 'success'
        env:
          RESOURCE_GROUP: ${{ steps.foundation.outputs.resource_group_name }}
        run: az containerapp job delete --name "$SEEDER_JOB" --resource-group "$RESOURCE_GROUP" --yes --output none

      - name: Upload plans and evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: goal006-${{ inputs.environment }}-${{ inputs.apply && 'apply' || 'plan' }}-${{ inputs.release_run_id }}
          path: |
            registry-release-manifest.json
            secret-seeder-execution.json
            infrastructure/terraform/phase2/environments/${{ inputs.environment }}/foundation/foundation.tfplan
            infrastructure/terraform/phase2/environments/${{ inputs.environment }}/workload/workload.tfplan
          if-no-files-found: warn
          retention-days: 90

      - name: Reauthenticate bootstrap identity for cleanup
        if: always() && env.RUNNER_IP != ''
        uses: azure/login@v2
        with:
          client-id: ${{ env.ARM_CLIENT_ID }}
          tenant-id: ${{ env.ARM_TENANT_ID }}
          subscription-id: ${{ env.ARM_SUBSCRIPTION_ID }}

      - name: Close state firewall rule
        if: always() && env.RUNNER_IP != ''
        run: |
          az storage account network-rule remove \
            --resource-group "$TFSTATE_RESOURCE_GROUP" \
            --account-name "$TFSTATE_STORAGE_ACCOUNT" \
            --ip-address "$RUNNER_IP" --output none