# ADR Quick Reference

**21 ADRs — one line each. Read only the full ADR if you need rationale or alternatives.**
**All status: Accepted. Date: 2026-07-07 (ADR-001 to 018) / 2026-07-08 (ADR-019 to 020) / 2026-08-01 (ADR-037 to 038).**

| ADR | Decision (one line) | Your constraint | Read full if... |
|---|---|---|---|
| **001** | gRPC for all CE internal communication | All calls to CE are gRPC — no REST to CE from any service | Implementing CE client or new RPC method |
| **002** | OpenAPI spec-first — spec is truth, never code-generated | Write spec before any endpoint code; use `openapi-generator-cli` for clients | Adding a new REST endpoint |
| **003** | `tenant_id` from JWT as sole multi-tenancy anchor → `SET LOCAL app.tenant_id` in every DB session | Never accept tenant_id in request body; always from JWT; DB interceptor is mandatory | Implementing JWT middleware or EF Core DbContext |
| **004** | Azure SignalR (cloud) / plain WebSocket (dev) for Emergency Stop | Emergency Stop path is pre-warmed WebSocket — never HTTP long-poll | Implementing Emergency Stop in any service |
| **005** | PAAS session affinity — one replica per session | PAAS sessions hold Decision Space in memory; replica crash = session lost (reload on first post-crash request) | Implementing PAAS Engine or session management |
| **006** | ASP.NET Core built-in rate limiting; no API gateway at MVI | Apply per-tenant rate limits at BP middleware layer; never on Emergency Stop path | Adding rate limiting to any endpoint |
| **007** | mTLS for CE gRPC in cloud; plain TLS in dev | CE uses mTLS in cloud — certificates auto-rotated; cert-manager in prod | Implementing CE client in cloud environment |
| **008** | Keycloak 25.0.6 as OAuth broker; Google default IDP; version pinned | Never bypass Keycloak; pin version; add upgrade protocol note for major updates | Changing auth flow or adding identity provider |
| **009** | OTel SDK in all services → Jaeger (dev) / Azure Monitor (cloud); OTLP_ENDPOINT env var | Every service emits OTel; OTLP_ENDPOINT is the only env change between envs | Adding observability to a new service |
| **010** | Azure-first with named escape hatches per Azure dependency | Document escape hatch for each Azure service used; no undocumented Azure lock-in | Introducing a new Azure service dependency |
| **011** | EF Core Migrations; append-only rule: no destructive migrations on constitutional schema | Never `DROP`, `ALTER TYPE DROP VALUE`, or `UPDATE` on constitutional schema in any migration | Writing DB migrations (read empty-migration technique in engineering-standards §9) |
| **012** | GHCR as container registry; GitHub Actions native | Push to GHCR on CI; tag with `sha-{git-sha}`; retag for promotion | Setting up image build or promotion pipeline |
| **013** | GitHub Actions CI/CD; trunk-based; CCTs as mandatory gate | CCT failure blocks ALL promotion — no exception, no manual override | Adding or modifying the CI/CD pipeline |
| **014** | `.env` dev / GitHub Secrets CI / Azure Key Vault cloud | Never commit secrets; Key Vault ref pattern: `secretref:` in Container Apps | Handling secrets in any environment |
| **015** | Temporal self-hosted (dev/QA) → Temporal Cloud (UAT/prod) | Dev: `temporalio/auto-setup:1.24` in docker-compose; prod: Temporal Cloud mTLS | Implementing Temporal workers or configuring Temporal |
| **016** | .NET 9 for CE + BP; Python 3.12 for PR + AI Runtime | Language is frozen — do not introduce polyglot additions without a new ADR | Never (frozen) |
| **017** | Next.js 14 TypeScript PWA Phase 1 → React Native Phase 2 | Emergency Stop always visible; strict TypeScript; use openapi-generator for API clients | Adding new UI framework or library |
| **018** | Emergency Stop signal routing via Temporal — PAAS sessions are `PAASSessionWorkflow` | CE sends Temporal signal by workflow ID; `active_session_ids` in EmergencyStopRequest are Temporal workflow IDs | Implementing Emergency Stop fan-out at scale |
| **019** | RAG Architecture — three-tier (Domain / Customer / Platform Intelligence) | pgvector in `institutional` schema at MVI; Customer context isolated per tenant | Implementing RAG pipeline or adding new agent domain knowledge |
| **020** | MCP Integration Pattern — AI Runtime is MCP client; each platform capability is MCP server | Every MCP tool call requires CE.ValidateAction FIRST (C-041); default deny in Tool Registry | Adding a new external platform or tool capability |
| **021** | External Platform OAuth — Server-side Auth Code flow; oauth-vault service (port 8130) manages token storage + refresh; MCP servers retrieve tokens from oauth-vault via mTLS | Instagram requires Professional account (not personal); token refresh failure → pause affected skill + notify customer | Adding a new external platform OAuth integration (ADR-021) |
| **022** | Razorpay India for all payments — Subscriptions API for recurring billing; razorpay-mcp sidecar (port 8131); GST invoicing mandatory (SAC 9984, 18%); pro-rata via billing event ledger | Budget ceiling enforcement via CE.ValidateAction with BudgetContext (C-043); payment failure → 3-day grace → suspend | Implementing billing, changing payment processor, adding a new pricing tier (ADR-022) **[Amended: ADR-034 — universal prepaid buckets, single onboarding payment, progressive renewal failure saga, C-090 grandfather pricing]** |
| **023** | WhatsApp Phone-as-Identity for C-042 Agents — phone number = Meta-verified identity; Phone Identity Service (port 8137); auto-registration on first message; HMAC webhook validation; TRAI opt-in enforcement | Replay attack prevention (±5 min timestamp); TRAI 24-hour service window; HSM-only outside window; no Keycloak for WhatsApp-native users | Adding a WhatsApp-native onboarding flow to any C-042 agent (ADR-023) |
| **024** | Token Economy — four-layer cost architecture: (1) Message Classification Gate (LOCAL tier, 60–70% zero-cost), (2) Model Tier Routing (FRONTIER/MID_TIER/LOCAL/FREE_BATCH per prompt), (3) Semantic Response Cache (pgvector, privacy-safe, 7–14 day TTL), (4) Pre-computation at 02:00 IST for scheduled outbound messages | Customer budget in UsageUnits (not tokens); emergency alerts always exempt from budget (C-001, C-051); DMA cost reduced 74%, Agricultural 66% per customer/month | Implementing any LLM dispatch path, adding a new agent type, changing model providers (ADR-024) **[Amended: ADR-034 — bundle ration gate added to PSE before LLM dispatch; pacing choice; downgrade-with-disclosure path]** |
| **025** | Daily-Refresh Broker Token — Indian broker APIs (Zerodha Kite, Upstox) use daily session tokens that cannot be background-refreshed; new DAILY_BROKER_TOKEN class in oauth-vault; customer must re-authenticate daily at 7 PM IST via deep link; 60-second exchange window handled by oauth-vault callback; BROKER_AUTH_EXPIRY is CRITICAL SIL signal | Never store customer broker password; Angel One TOTP model deferred; billing credit for auth-failure-missed sessions is open question | Implementing broker API integration for trading agents; any daily-expiry token pattern (ADR-025) |
| **026** | Centralized Ad Account Management — WAOOAW MBM + Google MCC; agency model; 10% management fee on gross ad spend; Ad Spend Wallet (prepaid, pass-through); two-part billing (subscription SAC 9984 + ad services SAC 998361); CUSTOMER_OWNED alternative PENDING_FOUNDER_AUTHORIZATION | C-056 (segregation, transparency, pass-through purity, read access, data sovereignty); C-043 (budget ceiling still applies); ad_spend_ledger is append-only (no UPDATE/DELETE) | Implementing Skill 11 Paid Advertising; any change to ad billing model or platform credentials (ADR-026) |
| **027** | Cloud Architecture Optimization — 10 cost optimizations; scale-to-zero on all non-CE services; Burstable B2ms for dev; Standard D2ds_v5 for prod; CE min=1 for trading SLA | C-067 (cost ceilings per environment); all Terraform modules | Cloud infrastructure sizing, scaling configuration |
| **028** | Steward and Subscription Tier LLM Separation — stewards always FRONTIER (bypass ADR-024 gate); customers routed by plan_tier JWT claim (essential/professional/enterprise); ADR-003 amended to add plan_tier + steward person claims | C-068 (Steward Access Isolation); C-051 (Resource Transparency — customers told their plan's tier); C-064 (Three-Human Institution) | Implementing LLM Gateway routing, adding subscription tiers, changing LLM providers (ADR-028) |
| **029** | Multi-Provider LLM Strategy + Conscious Provider Selection — Google Gemini 2.0 Flash (MID_TIER primary, Vertex AI Mumbai), Sarvam AI Saaras (Agricultural MID_TIER override, C-042), Gemini 2.5 Pro (FRONTIER primary); AI4Bharat IndicBERT (LOCAL Indian language); Azure fallback chain; Provider Selection Engine: Rule Layer (PSE-R01 to PSE-R08) + Performance Layer (composite_score from institutional.provider_dispatch_events); 40% cost saving vs single-provider; DPDPA primary position | C-042 (Vocabulary Mandate — regional language fidelity), C-051 (Resource Transparency), C-069 (Self-Improvement — conscious selection from evidence); ADR-024 amended (provider dimension); FA-021 (GCP Vertex AI), FA-022 (Sarvam AI) | Any LLM provider change, adding new agent type, DPDPA compliance review, cost optimisation (ADR-029) |
| **030** | RESERVED — Zero-Cost Dev Agent Architecture (IB-020) — ADR-030 spec pending Founder authorization of C-077 (Development Tooling Cost Ceiling). Do not use this number for another ADR. | C-077 (DRAFT — Development Tooling Cost Ceiling); IB-020 (zero-cost dev agent design) | Implementation of IB-020 autonomous sprint agent |
| **030** | Autonomous Sprint Agent Code Generation (IB-020) — Claude Sonnet 4.6 for `model_hint: reasoning` tasks; 3-attempt retry loop with validation; `<file path="...">` response format; write boundary enforcement (src/**, tests/**); CCT gate after generation; flag_spec_gap on exhausted retries; C-077 cost ceiling via Anthropic $35/month console limit | C-059 (Traceability), C-065 (SDLC Author hat), C-073 (Annotations), C-076 (≥90% coverage), C-077 (Dev cost ceiling ₹5,000/month); IB-020 | Implementing WC-012 through WC-018 code generation tasks (ADR-030) |
| **031** | CE Fail-Safe Behavior on Unavailability — when CE gRPC health probe fails 2 consecutive checks, BP returns 503 on all write endpoints; PR pauses all PAAS execution loops; Emergency Stop executes locally and buffers for CE recovery; local audit buffer (max 1,000 records) holds governance events during outage; automatic recovery within 30s of CE returning SERVING; constitutional.engine_availability_events table records every outage event | C-079 (CE Fail-Safe Halt — RATIFIED 2026-07-23); C-023 (Evidence First — no action without CE); C-001 (Human Override — Emergency Stop unaffected); no bypass mode, no degraded execution mode | Implementing CE health probes, BP circuit breaker, PR session halt logic, local audit buffer, CCT-CE-AVAIL-01 chaos test (ADR-031) |
| **032** | RESERVED — Mobile Application Architecture — Flutter 3 / Dart; CE enforcement on mobile; offline halt behavior (C-079 mobile extension); push notification Emergency Stop path (C-001 mobile extension); biometric auth constitutional position (C-003); device PII surfaces (C-078 mobile extension); CE enforcement boundary on mobile (C-066 floor). Not authorized until WC-018 complete and mobile IB item is approved by Founder. | C-001 (mobile extension), C-066 (CE boundary floor), C-078 (mobile PII), C-079 (mobile offline halt), C-081 (pubspec.yaml reference required) | Mobile Work Contract authorization — DO NOT write this ADR until mobile IB item is approved |
| **033** | MagicLLM Phase 2: Gemini Flash for Cat. 7-13 (Orchestration + Semantic) — `gemini-2.0-flash` on Vertex AI `asia-south1` (Mumbai) for SEMANTIC_UNDERSTANDING (1M token context), RESEARCH_QUERY, GOAL_UNDERSTANDING, ROUTING_INTELLIGENCE, JOURNEY_MONITOR, RESEARCH_ORCHESTRATION, DECISION_SYNTHESIS; OAuth2 JWT Bearer auth via SA key; 34× cheaper than Sonnet for high-frequency orchestration calls; India data residency (DPDPA); Cat. 1-6 unchanged on Anthropic Claude | C-059 (Evidence First), C-069 (Self-Improvement), C-077 (Cost Ceiling), C-042 (Vocabulary Mandate), ADR-029, ADR-030 | Any change to Gemini model version, adding Cat. 7-13 capabilities, Vertex AI region change (ADR-033) |
| **034** | WAOOAW Billing Engine (WBE) — dedicated Python 3.12 FastAPI service (port 8140) centralising all billing logic; 5 sub-components: Wallet Engine (one wallet/multiple buckets, ≤50ms balance check, Redis cache), Markup Engine (3-layer: thread markup → bundle cost floor → platform margin, C-089 floor enforcement), Usage Meter + Alert Engine (50/60/85/95% thresholds, quiet hours, Proactive Offer Engine), Platform Procurement Ledger (WAOOAW's own provider spend + runway projection + FA auto-generation), Reconciliation Engine (daily self-audit: discrepancy > 1 paise → billing halt); agent-agnostic (reads Agent Billing Profiles per C-088); Thread Catalog is single source of truth per C-091; ADR-022 + ADR-024 amended | C-088 (Agent Billing Profile), C-089 (Minimum Margin Floor), C-090 (Grandfather Pricing), C-091 (Thread Catalog Sovereignty), C-038, C-051, C-048 | Adding new agent billing profile, changing thread costs, modifying bundle rations, implementing WBE service (WBE-S1→S8), any pricing change (ADR-034) |
| **035** | Platform-Agent Contract (PAC) Standard — every agent spec must include a machine-readable PAC YAML block declaring: which platform signals it handles (AsyncAPI 3.0-aligned), schema version of each signal, graceful degradation behavior per service, budget vocabulary in customer terms, trial/live mode profile; Gap Scanner (scripts/gap_scanner.py) detects agents below current Agent Base Spec version; signal versioning: MINOR = backward-compatible, MAJOR = all PAC consumers must update; industry alignment: AsyncAPI 3.0 signal schema format, CloudEvents 1.0 envelope, Pact.io consumer-driven contract pattern | C-094 (Agent Base Spec Compliance), C-070, C-049, C-088, ADR-034; update PAC when: new platform component added, signal schema MAJOR bump, base spec version bump | Any new agent spec (PAC is gate section), adding platform component (create signal schema + run Gap Scanner), signal schema version change (ADR-035) |
| **036** | EA Skeleton Standard — every new platform service must have an EA-produced Code Skeleton (abstract base classes, data models, exceptions, router stubs — NO logic) before any implementation sprint fires; three-phase sprint model: SKELETON (EA) → IMPLEMENTATION (Platform IT Expert) → CCT; 75% token reduction, 100% type-error elimination (SIM-PLATFORM-001 30/30 PASS); skeleton files live in src/{service}/skeleton/ (EA-owned); compile gate blocks implementation if skeleton missing; hard boundary: EA produces contracts, sprint fills bodies | C-095 (Component Manifest Obligation), C-059 (Traceability), ADR-035, ADR-036; task_type field in WC; 6 backward-compatible pipeline changes | Any new platform component (EA must produce skeleton before PO creates implementation WC), pipeline upgrade (PL-S1..PL-S6), blueprint assurance run (ADR-036) |
| **037** | Sprint Environment Contract Validation — `scripts/env_validator.py` runs as a dedicated CI step after dependency install, before the groomer; scans all `tests/` imports via AST; validates each non-stdlib module is importable via `python -c "import X"`; exits 1 on any gap → sprint halts before LLM tokens are spent; `requirements-test.txt` is the single authoritative dependency source (replaces hardcoded pip install list); resolves C-032 drift between declared authoritative source and actual CI install | C-032 (Spec/Code Drift), C-086 (Pre-Execution Gate), C-077 (Dev Cost Ceiling) | Adding new test dependency (update requirements-test.txt), CI environment changes, any new test library introduced by a WC |
| **038** | Multi-Stack Compile Gate Architecture — three-layer defence: Layer 0 (`_PYTHON_FORBIDDEN_PATTERNS`, `_TYPESCRIPT_FORBIDDEN_PATTERNS`, `_TERRAFORM_FORBIDDEN_PATTERNS` + Pipeline Self-Model in SYSTEM slot), Layer 1 (ruff check + sqlfluff + yamllint + biome + hcl2 parse inside 3-attempt retry loop in `ResponseEvaluator._gate_compile()`), Layer 2 (`sprint-context/lint-violations.json` learning cache written on gate failure, read at prompt time); `sprint_retry_advisor._classify_ruff_violation()` produces combined fix for all ruff codes in one response (ANN201, ANN001, B017, B006, F841, B018, G004, E501); regex `[A-Z]{1,3}\d{3,4}` ensures multi-letter codes like ANN201 are extracted correctly | C-082 (Build Validation — all stacks), C-069 (Self-Improvement — violation cache), C-077 (zero extra LLM cost), C-032 (forbidden patterns in SYSTEM slot) | Python subtask writing a service file, adding a new stack/language, SQL/YAML/Terraform compile gate configuration, tuning ruff forbidden patterns (ADR-038) |
