# requirements-udcp.txt
# constitutional_basis: C-080 (Docker Test Isolation)
# Minimal Python deps for the UDCP standalone harness.
# All UDCP engines use Python 3.12 stdlib only — these are test/lint extras.

# ── Test framework ───────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100               # C-097 property-based tests

# ── Lint ─────────────────────────────────────────────────────────────────────
ruff>=0.5

# ── MagicLLM auth (live mode only — Vertex SA key path) ─────────────────────
PyJWT>=2.8
cryptography>=42.0
