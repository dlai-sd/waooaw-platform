# requirements-udcp.txt
# constitutional_basis: C-080 (Docker Test Isolation)
# Minimal Python deps for the UDCP standalone harness.
# All UDCP engines use Python 3.12 stdlib only — these are test/lint extras.

# ── Test framework ───────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23            # asyncio_mode = "auto" config in pyproject.toml
hypothesis>=6.100               # C-097 property-based tests
httpx2>=0.28                    # Starlette TestClient; httpx alone raises deprecation error under filterwarnings=error
psycopg2-binary>=2.9            # root tests/conftest.py imports psycopg2 at collection time

# ── Service deps needed to import generated code under test ──────────────────
fastapi>=0.100                  # generated tests define mock FastAPI routers
sqlalchemy>=2.0                 # BundleEngine uses text() + Session

# ── Lint ─────────────────────────────────────────────────────────────────────
ruff>=0.5

# ── MagicLLM auth (live mode only — Vertex SA key path) ─────────────────────
PyJWT>=2.8
cryptography>=42.0
