# WC-084 Capability Discovery And Reuse Ledger

**Implements:** `work-contracts/WC-084-auth-readiness-and-customer-portal-plan.md` Section 9.4  
**Executor:** Platform IT Expert (INST-010)  
**Baseline:** PR #406 merge `ee84c11fdcde1b7a2d8afe062c64b1fc52dee76e`  
**Closure update:** INST-005 consolidated owner-contract closure, 2026-09-08
**Authority boundary:** This ledger now records the accepted WC-084 Customer Portal solution closure. It does not authorize runnable implementation, provider activation, deployment, or Founder visual acceptance.

| UI capability and state | Accepted contract and owner | Disposition | Client impact and constitutional rules | Channel mapping | Required decision or approval |
|---|---|---|---|---|---|
| Authenticated shell: ready, unauthorized, expired, step-up, unavailable | BP `GET /api/v1/identity/session`; Identity Boundary / INST-005 | `REUSE` | Generated `IdentityApi`; bearer token remains server-only; BP derives tenant, roles, capabilities, assurance and next action; `no-store`; no JWT capability inference | Same projection semantics for web, WhatsApp adapter and future mobile | None for this slice |
| Session termination and protected-state cleanup | `architecture/reference/components/identity-boundary.md`; `architecture/reference/components/customer-portal-solution-contract.md`; Identity owner / INST-005 | `REUSE + AMEND` | No BP sign-out endpoint is introduced; sign-out remains the Keycloak/NextAuth boundary with deterministic cleanup of protected drafts, cache, optimistic state, and account-scoped storage; profile/settings/login-method reads are now canonicalized | Shared termination and cleanup semantics remain channel-neutral; web performs local cleanup, other channels preserve relationship truth | Implementation gate only: environment/provider readiness and Founder visual acceptance where applicable |
| My Agents list and resume target | Relationship Workspace + `GET /api/v1/employment/relationships`; BP and professional owners / INST-005 | `AMEND` | Generated clients now receive a tenant-derived paginated relationship-summary collection and server-selected resume target; no browser resume heuristic | One relationship list and resume target grammar for web, WhatsApp, and future mobile | None within API architecture |
| Conversation: loading, timeline, send, retry, read, stream, cancel | WC-060 BP conversation contract; BP / INST-005 | `REUSE` | Generated `ConversationApi`; relationship authorization, idempotency, evidence and reconciliation remain server-owned | Accepted neutral continuity envelope | No API decision; UI waits for an authorized relationship identifier |
| Voice: permission, capture, review, correction, send, cancel, erasure | WC-062 voice-contribution contract; BP / INST-005 | `REUSE` | Generated `VoiceContributionsApi`; consent, retention and Evidence First unchanged | Same contribution lifecycle across approved channels | No API decision; UI waits for an authorized relationship identifier |
| Onboard and Induct | Agent Broker disclosure/lifecycle, relationship workspace, configuration and conversation contracts; Product, BP, professional, Data, Security / INST-005 | `REUSE + CREATE` | No browser-local induction truth remains; canonical configuration read now returns exactly `Onboard` and `Induct`, with `Induct` mapped to accepted relationship and conversation truth rather than a new lifecycle state | Same continuation semantics across web, WhatsApp, and future mobile | None within API architecture |
| Goal Setting: skill selection, measure, frequency, verification, amendment, history | Skill and relationship goal/workspace capabilities plus `GET /workspace/goals`; Product, BP, professional, Data, Security / INST-005 | `REUSE + CREATE` | Active goals carry skill, measure, frequency and pending customer verification; amendment and replacement remain typed BP commands; no browser-owned eligibility | Universal goal semantics stay shared across channels | **BLOCKED:** the accepted typed command union has no customer goal-verification command, so `VERIFIED` and `REQUIRES_RENEWAL` cannot be written without an owner-approved contract addition |
| Business Outcomes: loading, stale, partial, corrected, unavailable | `GET /workspace/business-outcomes`; Product, BP, professional, Data, Security / INST-005 | `CREATE` | Outcome projection is now canonical: skill and goal traceability, measure, frequency, status, evidence, attribution limits, and explicit separation of agent performance from external outcome; no guarantee or browser calculation | Shared outcome grammar for web, WhatsApp summaries, and future mobile | None within API architecture |
| Operations: locked, eligible, active, goal amended | Relationship workspace/work plus `GET /workspace/operations`; Product, BP, professional, Data, Security / INST-005 | `REUSE + CREATE` | Eligibility is now server-projected from verified goals; goal amendments trigger explicit reassessment state instead of browser inference | Same lock, eligibility, and reassessment semantics across channels | None within API architecture |
| Marketplace browse and trial/hire | `GET /api/v1/professionals/marketplace` plus existing disclosure/trial/contract/activation operations; Product and BP / INST-005 | `REUSE + CREATE` | Browse pagination, filter, offerability, trial, and hire continuation are canonical; the portal follows the approved disclosure/registration journey and does not synthesize the evaluation-intent or relationship identifiers required by consequential operations | Shared browse and lifecycle semantics for web, WhatsApp continuation, and future mobile | None within API architecture |
| Alerts feed, read and acknowledge | Relationship attention + `GET/POST /api/v1/notifications/alerts...`; Product, BP, Data, Security / INST-005 | `REUSE + CREATE` | Cross-relationship ordered feed, stable destination, read, and acknowledge semantics are now canonical; read is not approval | Shared ordered feed and cursor lineage across channels | None within API architecture |
| Billing projection | Existing billing operations plus `GET /api/v1/billing/summary`; WBE and BP / INST-005 | `REUSE + CREATE` | Portal billing requires a BP-mediated WBE summary for allowance, forecast, assumptions, payment state, preference, invoices, and consequences; browser performs no monetary calculation | Same commercial truth for every channel | **BLOCKED:** the implemented WBE owner gateway exposes only relationship actuals, forecast, thresholds and currency state; no owner operation supplies required customer-global preference, payment, allowance, or invoice fields |
| Profile and settings | Identity session, notification preferences, and new identity profile/settings/login-method operations; Product, Identity, BP, Data, Security / INST-005 | `REUSE + CREATE` | Canonical profile/settings truth now exists for customer, organization, locale, theme, channels, security actions, and login-method summaries; no browser-local account truth | Shared profile/settings semantics; channel-specific presentation only | None within API architecture |

## Executable Gate Evidence

- Canonical generated-client reconciliation: focused Docker Jest contract tests, 2 suites and 6 tests passed.
- BP session adapter and shell: focused Docker Jest tests, 2 suites and 16 tests passed.
- WC-083 baseline: Docker build, typecheck, 9 provider tests and 188 Jest tests passed; browser matrix recorded 19 passes and one WebKit page-setup timeout before assertions. The exact WebKit case passed on isolated Docker rerun.
- Identity portal runtime: 111/111 Dockerized Identity tests passed; WC-084 migration guards passed 4/4.
- Relationship collection and workspace lifecycle reads: focused Docker suites passed 18/18; My Agents is participant-authorized, Configuration returns exactly Onboard and Induct, and Operations remains server-locked while goal verification is unavailable.
- Marketplace and Alerts: focused Docker suites passed 11/11; catalog ordering/filter cursors are server-owned, and alert read/acknowledge mutations are tenant-bound, version-bound, and idempotent.
- Customer Portal web: strict Docker TypeScript passed; full Docker Jest passed 37 suites and 193 tests; the Docker WC084 Playwright matrix passed 18 tests across Chromium expanded/intermediate/360, Firefox, and WebKit with two intentional Chromium-only reflow skips.
- Interaction evidence: browser acceptance executes Marketplace disclosure continuation, Alert acknowledgment, Profile save, Settings save, account-drawer access, and Onboard save through same-origin server boundaries backed by generated clients.

The selected portal scope is implemented except where owner contracts are absent. Goal-gated Operations cannot reach `ELIGIBLE` until the typed-command contract adds an explicit customer goal-verification mutation; implementation does not alias that consequential action onto `AMEND_GOAL`, `REPLACE_GOAL`, or browser state. Billing remains truthfully unavailable until WBE supplies the customer-global projection.
